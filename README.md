# adversarial-inputs-detectors
IDE plugins that detect malicious content in documentation and code files that can exploit AI coding assistants.
- VSCode plugin source code can be found in the `vscode` directory.
- IntelliJ plugin source code can be found in the `intellij` directory.
